class MovieModel {
  int id;
  String title, image, description;
  var genres;
  var rate;

  MovieModel({
    this.id,
    this.title,
    this.image,
    this.genres,
    this.description,
    this.rate,
  });
}
